---
- hosts: localhost
  become: yes
  become_user: clacko
  pre_tasks:
    - name: downloading files to jenkins
      get_url:
        url: https://artifacts.elastic.co/downloads/beats/metricbeat/metricbeat-6.0.1-x86_64.rpm
        dest: /tmp
  
- hosts: linux
  become: yes
  become_user: clacko
  become_method: sudo
  roles:
    - role-beatinstall

#  post_tasks:
#     - shell: vim /tmp/testfile2



#  tasks:
#    - name: test connection
#      ping:


#- hosts: windows
#  remote_user: clacko
#  tasks:
#    - name: test connection
#      win_ping:
#

